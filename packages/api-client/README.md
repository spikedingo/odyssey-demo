# @odyssey/api-client

This package is **generated** by Orval from the backend OpenAPI spec.

- Run `pnpm gen:contract` from the workspace root to regenerate.
- Do **not** hand-edit files in `src/generated/`.
- The `src/generated/` directory is gitignored.
